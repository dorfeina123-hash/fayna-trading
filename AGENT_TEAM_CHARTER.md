# צוות הסוכנים של Fayna Trading
### מסמך הפעלה · נטען בתחילת כל סשן עבודה

> **קרא אותי לפני שאתה נוגע בקוד.**
> המסמך הזה קיים כי כל סשן של Claude מתחיל ללא זיכרון. הוא מחליף את הזיכרון
> החסר: מי הצוות, מה כל אחד בודק, ומה כבר הוחלט.

---

## 0. המגבלה שאין לעקוף בניסוח

Claude אינו רץ ברקע. אין תהליך שמתעורר לבד. ביקורת "שלוש פעמים ביום"
מתבצעת רק דרך **משימה מתוזמנת** בסביבה, או בתחילת כל סשן.
אין להציג ביקורת שלא בוצעה כאילו בוצעה.

---

## 1. הצוות

| סוכן | תחום | מה הוא בודק בפועל |
|---|---|---|
| 🏗️ ארכיטקטורה | מבנה וגדילה | צמתי DOM בטעינה, מסכים כבדים, מודולריות, מוכנות לשרת |
| ⚡ ביצועים | מהירות ומשאבים | טיימרים ללא ניקוי, מאזינים, `innerHTML` בנתיבים חמים, גודל טעינה |
| 🔐 אבטחה | חשיפה וסיכון | XSS, סודות בקוד, חוקי Firestore, הרשאות, `rel=noopener` |
| 🧹 קוד | תחזוקתיות | קוד מת, כפילויות אמיתיות, פונקציות שנקראות ולא קיימות |
| 🎨 UI | עקביות ויזואלית | פלטה אחידה, סקאלת טיפוגרפיה, ריווחים, סגנונות inline |
| 🧭 UX | זרימה ונוחות | צפיפות פעולות למסך, מצבי ריק/טעינה/שגיאה, היררכיה |
| ♿ נגישות | WCAG 2.2 AA | alt, ARIA, ניגודיות, ניווט מקלדת, גדלי מגע |
| 🔍 SEO | גילוי אורגני | meta, Open Graph, JSON-LD, robots, sitemap |
| 🗄️ מסדי נתונים | Firestore | שאילתות ללא `limit`, N+1, אינדקסים, עלות לקריאה |
| 🔌 API | אינטגרציות | טיפול בשגיאות, backoff, טוקנים, latency |
| 🤖 AI | סוכנים | בריאות ה-runtime, כשלים, יכולות שהוכרזו |
| 📈 צמיחה | משתמשים והכנסה | חסמי הרשמה, מסלול הפניות, המרה, נטישה |

---

## 2. פרוטוקול הביקורת

```
1. סרוק      — הרץ מדידות אמיתיות על הקוד. לא הערכות.
2. סנן       — כל ממצא נבדק לפני שהוא מדווח (ראה §3).
3. דרג       — 🔴 שובר · 🟠 משמעותי · 🟡 שיפור · ℹ️ מידע
4. תקן       — בטוח ומאומת → בצע. משנה התנהגות → הצג לאישור.
5. אמת       — תחביר, IDs כפולים, שלמות CSS. תמיד.
6. דווח      — מה תוקן, מה ממתין, ומה נדחה ולמה.
```

---

## 2ב. פרוטוקול הפריסה — **Claude מעלה, לא המשתמש**

> החלטת המשתמש (v45): "אני רוצה שתעדכן את הכל באתר שלי מעתה והלאה, כל הזמן
> אתה זה שמעדכן את זה." אין להחזיר לו הוראות העלאה ידניות.

**מה מותר להבטיח:** בתוך סשן פעיל, כל שינוי בקוד נגמר בהעלאה מאומתת.
**מה אסור להציג כאילו קורה:** מעקב רציף, העלאה בין סשנים, ריצה ברקע.
Claude אינו מתעורר לבד. אם דרוש עדכון כשאין סשן — זו משימה מתוזמנת.

```
1. שנה     — ערוך את index.html + dor_trading_v<N>.html (זהים)
2. אמת     — תחביר לכל בלוק script · איזון סוגריים ב-CSS · 6 בלוקי style ·
             הצהרות ברמה עליונה לפני/אחרי · הרצת הרתמה ב-Node
3. העלה    — github.com/dorfeina123-hash/fayna-trading/upload/main
4. ודא     — צלם מסך לפני Commit; אשר ששני הקבצים ברשימה
5. קומיט   — הודעה קצרה מ-50 תווים, אחרת הכפתור זז והלחיצה מחטיאה
6. המתן    — ~45 שניות ל-Action + Pages
7. אמת חי  — fetch לקובץ מהשרת; חפש סימן ייחודי מהשינוי האחרון
8. בדוק    — נקה SW + caches, טען מחדש, הפעל את הפיצ'ר באינטראקציה אמיתית,
             קרא קונסול. **לא לכפות מצב ידנית — זה מדלג על הקוד השבור.**
```

**קבצים שתמיד עולים יחד:** `index.html` + `dor_trading_v<N>.html`
(ה-Action בוחר את הגרסה הגבוהה ומעתיק אותה על index.html — אם רק index.html
יעלה, ה-Action ידרוס אותו בגרסה ישנה יותר).

**⚠️ בדוק תמיד לפני העלאה: האם `index.html` המקומי מקדים את קובץ הגרסה?**
זה קרה ב-v47: `index.html` הכיל תיקון הרשמה דרך Google שלא היה ב-v46.
העלאה נאיבית של השניים הייתה גורמת ל-Action להעתיק את v46 על index.html
ולמחוק את התיקון. הפתרון: `cp index.html dor_trading_v<N+1>.html` וליישר.

**מודולים נפרדים (מ-v47):** `fayna-metrics.js`, `fayna-merge.js` עולים
כקבצים עצמאיים. הם **אינם** ב-PRECACHE של `sw.js` בכוונה — זה יבטל את
הטעינה העצלה. הם נשמרים תחת כלל הנכסים (cache-first), וה-`activate` מוחק
כל מטמון שאינו מתחיל ב-VERSION הנוכחי — **ולכן חובה להעלות VERSION בכל
פריסה**, אחרת מודול מעודכן לא יגיע למשתמש.

---

## 3. מלכודות שכבר נפלנו בהן

**אלה לא תיאוריות. כל אחת מהן קרתה בפרויקט הזה.**

| מלכודת | מה קרה | הכלל |
|---|---|---|
| "פונקציה מתה" | IIFE כמו `(function _x(){})()` נספרה כלא-בשימוש | בדוק IIFE לפני מחיקה |
| "פונקציה כפולה" | `window.x = function` נספר כהגדרה שנייה | בדוק סקופ לפני איחוד |
| סקריפט שמוחק CSS | regex שגוי מחק 174KB של עיצוב | **תמיד** ספור בלוקי `<style>` לפני ואחרי כתיבה |
| `#id` בלי `.active` | סלקטור ID גבר על `.tp{display:none}` — במובייל אף לשונית לא נפתחה | חשב specificity, אל תניח |
| `overflow:hidden` על header | חתך את כל התפריטים שנפתחים מתוכו | בדוק מה יוצא מגבולות ההורה |
| `canvas{height:auto}` | שבר את Chart.js — הגרפים תפחו ודחפו את הפריסה | אל תדרוס מידות שספרייה מנהלת |
| `touch-action:none` על body | מגירה פתוחה הקפיאה את כל המסך | נעילת גלילה ≠ ביטול מגע |
| קריאה לפונקציה שהוסרה | `_applyEdTexts` נמחקה, נשארו 2 קריאות → שגיאת התחברות | סרוק קוראים לפני מחיקה |
| חיתוך בלוק שגלש | פאץ' v45 חתך עד `\n\n` וחטף איתו `let _bizViewDate` שאחרי הבלוק → `renderBiz` קרס | **אחרי כל החלפת בלוק: השווה את קבוצת ההצהרות ברמה העליונה לפני ואחרי.** בדיקת תחביר לא תופסת הצהרה חסרה |
| `position:fixed` בלי היסטים | שכתוב `toggleNotifPanel` הסיר את קוד המיקום. הפאנל נפתח ב-y=1371 כשהמסך 1215 — מתחת לקצה, בלתי נראה | אלמנט `fixed` **חייב** `top`/`left` מפורשים או ברירת מחדל ב-CSS |
| אימות שמדלג על הבאג | בדקתי את הפאנל ע"י `classList.add('open')` + קואורדינטות ידניות. זה בדק רינדור ודילג בדיוק על הקוד השבור | **אמת דרך אינטראקציה אמיתית** (`.click()`), לא ע"י כפיית המצב |
| בדיקת DOM על דף ממטמון | `fetch(cache:'reload')` הראה שהתיקון בשרת, אבל ה-DOM שנבדק היה של דף ישן → "תקין" על קוד שגוי | לפני בדיקה ויזואלית: `unregister()` ל-SW + `caches.delete()` + טעינה מחדש |
| קומיט שלא נשלח | לחיצה על Commit לפני שההעלאה הסתיימה, והודעה >50 תווים שהזיזה את הכפתור | צלם מסך לפני הלחיצה; ודא ברשימת הקבצים; אמת בהיסטוריית הקומיטים |
| "27 שאילתות ללא limit" | הספירה כללה `db.collection(x).doc(y)` — קריאת מסמך יחיד שלא צריכה `limit` | ספור רק קריאות ברמת אוסף: `\.collection\([^)]*\)(?!\s*\.doc)`. המספר האמיתי: **9** |
| "כפתור ריק בלי aria-label" | 12 כפתורי `.nt` ריקים — אבל הם בתוך `<div style="display:none">` עם הערה "hidden for backward compat with sw()" | בדוק את **ההורה** לפני שמדווחים על a11y. אלמנט מוסתר אינו ליקוי נגישות |
| "פונקציה נקראת ולא קיימת" | הסורק דיווח על `_applyTickerHTML`, `adminChangePlan` ועוד — כולם הופיעו רק בתוך הערות `/* v29: X() removed */` | הסר הערות מה-JS לפני ניתוח קריאות. הערת מחיקה מכילה את שם הפונקציה |
| "פונט נטען פעמיים" | ה-`<link>` השני הוא בתוך `<noscript>` — גיבוי, לא כפילות | קרא את התג העוטף, לא רק את התג עצמו |
| **XSS מאוחסן דרך שדה שהמשתמש כותב** | `loginLogs` נכתב מהלקוח עם `name` מטופס ההרשמה, ורונדר **גולמי** בפיד האדמין. משתמש שנרשם בשם `<img src=x onerror=...>` הריץ קוד בסשן של האדמין | כל שדה שמשתמש **אחר** כתב חייב `escHtml` לפני `innerHTML`. טבלת המשתמשים כן הייתה מוגנת — הפיד נשכח |
| **תיקון XSS שדילג על הפיד השכן** | v46 תיקן את `loginLogs`. `adminLogs` — פיד תאום, באותו מסך, עם אותו מבנה — נשאר גולמי. `_logAdminAction` שותל בו `_udpCurrentUser.email/.name` מהמסמך של המשתמש, וחוקי Firestore מצמידים את `email` רק ב-`update`, לא ב-`create` | כשמתקנים XSS בפיד אחד — **סרוק את כל האוספים שאדמין מרנדר**: `loginLogs`, `adminLogs`, `mailingLogs`, `billing_requests`, `waitlist`, `dnc`, `broadcasts`. תיקון נקודתי מותיר את התאום פתוח |
| **ערך "מאובטח" בחוקים שמאובטח רק בחצי** | `allow update` ב-`users` מצמיד `email` ל-`resource.data.email`. `allow create` **לא** מאמת אותו — משתמש יכול להיוולד עם `email` שהוא HTML | בדוק `create` ו-`update` בנפרד. הצמדת שדה ב-`update` לא מגינה על הערך ההתחלתי |
| grep לגרסה תפס נתיב SVG | `grep -o "v4[0-9]"` החזיר "v48" פעמיים — שתיהן בתוך `d="...M46 24v48..."` של אייקון Google | סמני גרסה מאמתים רק מול הערות `<!-- ... -->` או `VERSION =`, לא מול grep חופשי |
| "פונקציה נקראת ולא קיימת" — סיבוב שני | הסורק דיווח על `renderBoard`, `setBp`, `refreshUI`, `emitTask` — כולן **מתודות של אובייקט** (`Agents.renderBoard`, `FB.setBp`), וכן על `fetch` בלי `method:'POST'` שהיה בתוך הערת v38 שמתעדת קוד שהוסר | לפני דיווח: חפש `שם:` ו-`שם(` בתוך ליטרל אובייקט, וודא שהממצא אינו בתוך `/* */` |
| **`window.X` על הצהרת `let` גלובלית** (v51) | מודול חיצוני כתב `window.tradesList = window.tradesList.concat(batch)`. הצהרת `let` ברמה העליונה של סקריפט **אינה** יוצרת מאפיין על `window` — היא ברשומה הלקסיקלית. הקוד היה יוצר משתנה חדש לגמרי, מציג "יובאו 40 עסקאות", והיומן היה נשאר ריק. אומת ב-`vm.runInContext`: הפניה לא-מוסמכת עובדת, `window.X` מחזיר undefined | ממודול חיצוני פנה למשתני המערכת **לא-מוסמכים** ועטופים ב-`typeof` — כמו שאר הקוד. `function` כן נכנס ל-`window`; `let`/`const` לא |
| **עמודות כפולות באותו שם קורסות** (v51) | דוח MetaTrader מכיל `Price` פעמיים (פתיחה וסגירה). בניית אובייקט לפי כותרות הותירה רק את האחרונה, וכל עסקאות ה-Short קיבלו מחיר כניסה שגוי — כלומר P&L שגוי, בשקט | ייחד שמות כותרות כפולות לפני בניית השורות. בדיקה על Short היא הבדיקה שתופסת את זה — עסקת Long נראית תקינה גם כשהמיפוי הפוך |
| "53 אינטרפולציות לא ממולטות" (v49) | סריקת `${user.x}` ללא `escHtml` החזירה 53 חשודים. רובם false positives: `showToast`/`customConfirm` (מרנדרים דרך `textContent`/`escHtml` פנימי), אובייקטי קונפיג סטטיים (`ti.desc`, `f.desc`, `step.title`), ותבניות מייל/PDF שמוגנות ב-§4. נשארו 4 אמיתיים בלבד | **עקוב אחרי ה-sink, לא אחרי הביטוי.** אינטרפולציה מסוכנת רק אם היעד הוא `innerHTML`/attr. בדוק את מימוש הפונקציה המקבלת לפני דיווח |

---

## 4. גבולות — מה אסור לעשות בלי אישור

- למחוק פיצ'ר, גם אם נראה לא בשימוש
- לשנות התנהגות התחברות, הרשאות או חיוב
- לפרסם חוקי Firestore בלי לקרוא קודם את הקיימים
- לגעת בתבניות מייל או PDF עם `var(--)` — הן לא מרנדרות משתני CSS
- להפעיל חיבור ברוקר לפני שקיים שרת שמחזיק את הסודות

---

## 5א. v47 — ליבת חישוב + איחוד סטטיסטיקות (פרוס ומאומת)

**`fayna-metrics.js`** — מקור אמת יחיד לכל מספר במערכת. פונקציות טהורות
בלבד: בלי DOM, בלי Firebase, בלי מצב. העמלה **מוזרקת** (`netOf`) ולא
מחושבת מחדש, כדי שכלל העמלה יישאר במקום אחד. 40 בדיקות ב-Node.

**החלטות שנבעו ממדידה, לא מהנחה:**
- `profitFactor` מחזיר `null` כשאין הפסדים — לא אינסוף ולא NaN.
- `avgR` מחושב **רק** על עסקאות עם סטופ, ומחזיר `rCoverage`. אין שדה כיוון
  בעסקה; long/short נגזר ממיקום הסטופ. סטופ בין מחירי הכניסה והיציאה →
  הכיוון לא ידוע → העסקה לא נכללת. **לא מנחשים.**
- `totalFees` מחזיר `null` — המערכת שומרת עמלות ו-fees כשדה אחד.
- **Monte Carlo הוא דגימה עם החזרה, לא ערבוב סדר.** ערבוב לא משנה את הסכום,
  ולכן ייצר `P5 = P50 = P95` — טווח שנראה אינפורמטיבי ואינו. הבדיקה תפסה זאת.

**`fayna-merge.js`** — מסך איחוד סטטיסטיקות. נטען על פי דרישה דרך
`_loadModule()` בכניסה ללשונית. ה-promise נשמר במטמון כי טעינה כפולה של
אותו קובץ תגרום ל-redeclaration של ה-const העליון ותזרוק.

**מאומת חי:** 27 כרטיסי KPI · 10 גרפים · מטריצת מתאם שגדלה עם הבחירה ·
What-If (×2 הכפיל בדיוק, איפוס החזיר לבסיס) · מטמון יציב · 0 שגיאות קונסול.

---

## 5ב. v48 — ביקורת יזומה (2026-08-10, משימה מתוזמנת, ללא משתמש נוכח)

**נקודת הפתיחה:** `index.html` היה זהה בייט-לבייט ל-`dor_trading_v47.html`.
נוצר `dor_trading_v48.html`, תוקן, אומת, והועתק חזרה ל-`index.html`.

**מה תוקן (שינויים שאינם משנים התנהגות):**

1. 🔴 **XSS מאוחסן בפיד פעולות האדמין (`renderAdminLogs`).** `x.action`,
   `x.details` ו-`e.message` רונדרו גולמיים ל-`innerHTML` — `x.details` גם
   בתוך `title="..."`, כלומר בריחה גם דרך `"`. המקור: `_logAdminAction`
   שותל שם `_udpCurrentUser.email` / `.name` מהמסמך של המשתמש. חוקי
   Firestore מצמידים את `email` רק ב-`update`; ב-`create` הוא שדה חופשי,
   ו-`name` אינו מוגבל כלל. **זה בדיוק הבאג של v46, בפיד התאום שנשכח.**
2. 🟠 **`drvRender` — הזרקה דרך העורך (חוב פתוח #8 נסגר).** `item.url` נכנס
   ל-`href` ללא סינון סכימה, ו-`name`/`desc`/`icon`/`color` ל-`innerHTML`
   ללא escaping. נוסף `_safeUrl()` ליד `escHtml` — רשימת היתר
   `http/https/mailto/tel`, קישור יחסי ועוגן עוברים, כל סכימה אחרת → `'#'`.
   14 בדיקות ב-Node עברו.
3. 🟠 **`href="${ml}"` ברשימת תפוצת המייל** — `mailto:` נבנה מ-`u.email`
   גולמי. עטוף ב-`escHtml`.
4. 🟡 **SEO:** נוסף `<meta name="robots" content="index,follow,max-image-preview:large,max-snippet:-1">`.
5. 🟡 **נגישות + SEO:** לדף היו **0 תגי `<h1>`** — עץ הכותרות התחיל ב-`<h2>`.
   נוסף `<h1 class="sr-only">` (המחלקה כבר קיימת ב-CSS, אפס שינוי ויזואלי).
6. ⚙️ `sw.js`: `VERSION` הועלה `fayna-v47` → `fayna-v48` (חובה לפי §2ב,
   אחרת `fayna-metrics.js` ו-`fayna-merge.js` יישארו במטמון ישן).

**אימות (לפני → אחרי):** 23 תגי script · 15 בלוקי JS, כולם `node --check`
תקינים · 6 בלוקי `<style>`, איזון סוגריים 0 בכל אחד · 1,032 מזהים,
0 כפולים אמיתיים · JSON-LD תקין · `innerHTML` 230 = 230 ·
הצהרות top-level: **תוספת אחת בלבד — `function _safeUrl`, אפס מחיקות** ·
`escHtml(` 117 → 127.

**מדידות אמת (v48, 1,420KB):** 5,422 אלמנטי DOM סטטיים · 2,473 סגנונות
inline · 13 `setInterval` מול 24 `clearInterval` · 96 `addEventListener`
מול 3 `removeEventListener` · 15 קריאות `fetch` · 3 `new Chart` מול 15
`.destroy()` · 221 קריאות `localStorage` · 0 `eval`/`document.write` ·
0 `target="_blank"` ללא `noopener` · 0 `<img>` ללא `alt` ·
**9** שאילתות Firestore ללא `limit` (זהה ל-v46 — לא גדל).

**מה נבדק ונמצא תקין — לא נגעתי:**
טבלת המשתמשים ופאנל פרטי המשתמש (`textContent`, או `escHtml` מפורש) ·
פיד `loginLogs` (תיקון v46 שריר) · 13 כפתורי `.nt` הריקים (בתוך
`display:none`) · אפס פונקציות חסרות — כל החשודות היו מתודות אובייקט או
טקסט בתוך הערות · חיבור הברוקר סגור (`BROKER_SYNC_ENABLED = false`).

---

## 5ג. v49 — ביקורת יזומה (2026-08-11, משימה מתוזמנת, ללא משתמש נוכח)

**נקודת הפתיחה:** `index.html` זהה ל-`dor_trading_v48.html` (md5 `12c11e7e…`).
נוצר `dor_trading_v49.html`, תוקן, אומת, הועתק ל-`index.html` (md5 `50a66124…`).
`sw.js` VERSION הועלה `fayna-v48` → `fayna-v49`.

**מה נסרק ונמצא תקין (הפעם, בניגוד ל-v48, אין ממצא חמור):**
- **כל האוספים שאדמין מרנדר נסרקו לפי כלל §3:** `mailingLogs` (escHtml מלא),
  `loginLogs` ו-`adminLogs` (תיקוני v46/v48 שרירים), `broadcasts` (רינדור דרך
  `Notify.renderList` — escHtml על title/body/label; `open()` מנווט רק
  ללשוניות קיימות, לא href), מודאלי Email/WA broadcast (escHtml).
  `dnc` ו-`waitlist` נטענים רק לבדיקות/ספירה — אין להם רינדור רשימה.
- `toast` מרנדר עם escHtml; `customConfirm`/`customPrompt` עם `textContent`.
- SEO: robots ✓ canonical ✓ description ✓ OG+alt ✓ JSON-LD תקין ✓ h1 ✓.
- 0 `target="_blank"` ללא noopener · 0 img ללא alt · 0 eval · 0 IDs כפולים
  סטטיים (3 ה"כפולים" הם template literals עם `${ti}`).

**מה תוקן (4 נקודות escaping, הכל self-XSS בדאטה של המשתמש עצמו — המשך
ישיר למדיניות v46):**
1. 🟡 `_refreshSymDatalist` — `cs.sym`/`cs.name` של סימבול מותאם ל-datalist.
2. 🟡 טבלת הוצאות חופשיות (`cx-tbody`) — `c.category`/`c.desc` גולמיים.
3. 🟡 כרטיס הוצאה חוזרת — `tmpl.toolName||tmpl.company` גולמי גם ב-`title="…"`
   וגם בטקסט, ליד אחים שכן ממולטים.
4. 🟡 רשימת הגדרות טיקר — `s.title`/`s.proName` (טיקר מותאם = טקסט חופשי).

**אימות (לפני → אחרי):** 15 בלוקי JS, כולם `node --check` תקינים · 6 בלוקי
`<style>`, איזון 0 בכולם · 958 הצהרות top-level — **אפס תוספות, אפס מחיקות** ·
`escHtml(` 127 → 136 · `innerHTML` 235 = 235 · `fayna-metrics.js` +
`fayna-merge.js` עוברים `node --check`, לא שונו.

**נצפה ולא טופל (מודע):**
- `dmDetailedPDF()` מרנדר `ac.name`/`r.company`/`r.note`/`c.desc` גולמי בדוח
  ה-PDF — דאטה של המשתמש עצמו + תבנית PDF (§4). ממתין לאישור.
- `tickerRemoveCustom('${s.proName}')` ב-onclick — proName עם גרש ישבור את
  ה-handler של השורה שלו בלבד (self, מסונן חלקית ע"י דרישת `EXCHANGE:SYMBOL`).
- תבניות web3forms (18442+) — עדיין גולמיות, §4 חוסם (חוב #11).

**⚠️ טרם נפרס:** v46–v49 מקומיים בלבד. מוכנים להעלאה: `index.html` +
`dor_trading_v49.html` (זהים, md5 `50a66124…`) + `sw.js` (`fayna-v49`) +
`fayna-metrics.js` + `fayna-merge.js`. **מהשורש, לא מ-`GITHUB_UPLOAD`.**

---

## 5ד. v50–v51 — Audit מלא + שלב 0 ושלב 1 (2026-08-12, באישור מפורש של המשתמש)

**הרקע:** בוצע Audit מלא של המערכת (מסמך `Fayna_Audit_2026-08-11.docx`, סעיפים
A–H) שמיפה מה קיים, מה חסר, ומה הדרך הנכונה לחיבור חשבונות. המשתמש אישר
מפורשות שלושה שינויים תחת §4: הקשחת חוקי Firestore, הסרת מסלול חיבור הברוקר
מה-UI, ונעילת Guard שאינה ניתנת לעקיפה — **גם על ידו עצמו**.

### v50 — שלב 0

**חוקי Firestore הוקשחו ב-`create`** (`firestore.rules`): `email` חייב להיות
זהה לאימייל שבטוקן, ו-`name` מוגבל ל-120 תווים. זה חוסם את **מקור** ה-XSS
המאוחסן שתוקן פעמיים בתצוגה (v46, v48) ולא רק את הביטוי שלו. **זה נשאר בתוקף.**

### v52 — מלאך שומר (Guard) הוסר לבקשת המשתמש

המודול נבנה ב-v50 ו**הוסר במלואו** ב-v52 לפי בקשה מפורשת ("לא רלוונטית לי").
אין "שאריות מכובדות": הוסרו בלוק הסקריפט (12.3KB), הפאנל (6.5KB), שלוש נקודות
הניווט, התווית, ה-hook ב-`sw()`, אימוץ החוקים מהענן, וה-wrapper שעטף את
`saveAddTrade`/`saveManualEntry`. סה"כ 19,493 תווים. ההסרה בוצעה בסקריפט שעוצר
אם קטע אינו נמצא בדיוק פעם אחת, ומאמת **אפס קוראים יתומים** מול תשע תבניות.

**הקפאת ה-Guard הוסרה גם מחוקי Firestore, וזה לא ניקוי קוסמטי:** אילו הכלל
היה נשאר, כל משתמש שבמסמך שלו יש `td_guard.lockedUntil` עתידי היה נחסם מכל
עדכון — כלל שאוכף פיצ'ר שאינו קיים. שדות `td_guard` שנשארו במסמכים הם כעת
דאטה אינרטית ומתעלמים ממנה.

**אימות (v51 → v52):** בלוקי JS 16 → 15 (הבלוק שהוסר), כולם `node --check`
תקינים · 6 בלוקי `<style>`, איזון 0 · מזהים 1,045 → 1,028, 0 כפולים ·
הצהרות top-level 960 = 960 (הבלוק היה IIFE ו-`window.Guard =`, לא הצהרות) ·
`innerHTML` 237 → 236 · `escHtml(` 136 = 136 · `firestore.rules` מאוזן.

### v51 — אשף ייבוא רב-פורמטי (`fayna-import.js`, 46KB, נטען על פי דרישה)

לשונית חדשה **📥 ייבוא עסקאות**. שישה פרסרים ייעודיים + CSV כללי:
**TopstepX/ProjectX · NinjaTrader 8 · MetaTrader 4/5 · IBKR Flex (CSV+XML) ·
Tradovate · Binance**. זיהוי פורמט אוטומטי לפי ניקוד כותרות, תצוגה מקדימה עם
KPI לפני כתיבה, מיפוי עמודות ידני כגיבוי, שיוך לחשבון יעד, ומניעת כפילויות
דו-שכבתית: מזהה הברוקר (חזק) ומפתח התוכן הקיים `_tradeKey`.

**67 בדיקות ב-Node** על הליבה הטהורה (פרסינג, תאריכים, מספרים, כפילויות,
מקרי קצה). שתי מהן תפסו באגים אמיתיים לפני הפריסה — ראה §3.

**אימות (v50 → v51):** 16 בלוקי JS, כולם `node --check` תקינים · 6 בלוקי
`<style>`, איזון 0 בכולם · 1,045 מזהים, 0 כפולים · הצהרות top-level
959 → 960 — **תוספת אחת (`_openImport`), אפס מחיקות** · `innerHTML` 236 → 237 ·
`escHtml(` 136 = 136 · `sw.js` VERSION → `fayna-v51` (חובה §2ב).

**קבצים לפריסה (מצב סופי v52):** `index.html` + `dor_trading_v52.html`
(זהים, md5 `71d12d25…`) + `sw.js` (`fayna-v52`) + `fayna-import.js`.
`firestore.rules` — ידנית בקונסולת Firebase, לא דרך GitHub.
**אין להעלות את `dor_trading_v50/51.html`** — הם מכילים את Guard שהוסר.

### מה עדיין חסום, ולמה

חיבור חי לברוקר (Connect Account → OAuth → סנכרון) **לא נבנה ולא ייבנה מהדפדפן**.
מפתח API או סיסמת ברוקר בקוד לקוח היא חשיפה של חשבון המסחר. הסדר הנכון,
כפי שנקבע ב-Audit: ייבוא קבצים (בוצע) → שרת מינימלי + ProjectX/IBKR ב-polling →
Tradovate OAuth + WebSocket → Rithmic. כל שלב עומד בפני עצמו.

---

## 5. מצב נוכחי (עודכן: v46 · ביקורת יזומה 2026-08-02)

**נבנה:** Command Center · AI Assistant · 4 מסלולי מנוי · מערכת הפניות ·
ניהול תפוצה והסרות · נגישות WCAG AA · מרכז משפטי · עורך ויזואלי ·
8 סוכנים אוטונומיים · דוח יומי במייל · **מערכת התראות מלאה (v45)**

**v45 — מודול `Notify`:** מקור אמת אחד להתראות. רישום סוגים שממנו נבנים
לבד מסך ההגדרות, שבבי הסינון ומפת האייקונים — סוג חדש לא דורש שינוי UI.
`Push` (Notification API דרך ה-SW) · `Sound` (WebAudio, ללא קובץ) ·
`Broadcast` (שידור אדמין עם מעקב מסירה וקריאה).

**כלל שנוסף ב-v45:** `showToast` הוא משוב על פעולה, לא התראה.
הגדרת "חלונות קופצים" מכבה התראות בלבד — אחרת כיבוי ההגדרה גורם למערכת
להיראות שבורה כי "נשמר בהצלחה" נעלם גם הוא.

**v46 — ביקורת אבטחה ואיכות (אוטומטית, ללא שינוי התנהגות):**
תוקן XSS מאוחסן בפיד ההתחברויות של האדמין (`l.name`, `l.email` רונדרו גולמי).
הוקשחו 7 נקודות `innerHTML` נוספות עם טקסט חופשי של המשתמש (שם סימבול,
חברה בהוצאות, הערות, תבניות הוצאה חוזרת). נוספו `og:image:alt` +
`twitter:image:alt`. אומת: 6 בלוקי `<style>`, איזון CSS 0 בכל בלוק,
15 בלוקי JS תקינים ב-`node --check`, **2229 הצהרות top-level זהות לפני ואחרי**.

**מדידות אמת (v46, 1,436KB):** 27 תגי script · 6 בלוקי style ·
2,472 סגנונות inline · 1,022 מזהים · 0 מזהים כפולים אמיתיים ·
229 שימושי `innerHTML` · 0 `eval`/`document.write` ·
0 `target="_blank"` ללא `noopener` · 0 `<img>` ללא `alt` ·
13 `setInterval` מול 24 `clearInterval`.

**חובות פתוחים:**
| # | פריט | חסם |
|---|---|---|
| 1 | 150KB מסכים שנטענים מיד (admin, custom) | סיכון רגרסיה — 106 ו-79 מזהים מקושרים |
| 2 | 2,472 סגנונות inline | עבודה ידנית ארוכה |
| 3 | **9** שאילתות Firestore ללא `limit` (המספר תוקן — ראה §3) | דורש מיפוי לכל שאילתה |
| 4 | אין שרת | חוסם: סנכרון ברוקר, cron אמיתי, טוקנים מוצפנים |
| 5 | Lighthouse לא הורץ מעולם | התוסף בדפדפן היה חסום רוב הסשן |
| 6 | 135 `div`/`span` עם `onclick` ללא `role`/`tabindex` — לא נגישים למקלדת | הוספת `tabindex` ל-135 אלמנטים משנה סדר טאב — דורש אישור |
| 7 | `waitlist.get()` נקרא כדי לספור בלבד (שורה 17214) | `.count()` דורש אימות תמיכה ב-SDK compat |
| 8 | ~~`drvItems[].url` ללא סינון סכימה~~ | ✅ **נסגר ב-v48** (`_safeUrl`) |
| 9 | `GITHUB_UPLOAD/` מפגר אחרי הקבצים הראשיים | ראה להלן — חמור יותר ממה שתועד |
| 10 | `<script>` מ-CDN ללא `integrity` (Chart.js, EmailJS) | hash שגוי משתק את הגרפים לגמרי — דורש אימות מול cdnjs לפני הוספה |
| 11 | 4 תבניות מייל web3forms מרנדרות `user.name/email/phone` גולמי | §4 אוסר נגיעה בתבניות מייל בלי אישור |
| 12 | 143 `div`/`span` עם `onclick` ללא `role`/`tabindex` (היה 135) | גדל עם מסך האיחוד — עדיין דורש אישור, משנה סדר טאב |
| 13 | 96 `addEventListener` מול 3 `removeEventListener` | רובם על `window`/`document` לכל החיים — צריך מיפוי לפני מסקנה |

**🚨 תיקיית `GITHUB_UPLOAD` אינה ניתנת לפריסה כמו שהיא:**
- `GITHUB_UPLOAD/index.html` = 1,569,421 בייט, נשמר 2.8 ב-15:16.
  `index.html` הראשי = 1,454,840 בייט מ-v48. **הן אינן אותה גרסה.**
- `GITHUB_UPLOAD/sw.js` = 6,200 בייט מול 6,697 בייט בראשי.
- **`fayna-metrics.js` ו-`fayna-merge.js` אינם בתיקייה בכלל.** לפי §2ב הם
  עולים כקבצים עצמאיים; פריסה מהתיקייה הזו תיתן 404 במסך האיחוד.
- המסקנה: **לפרוס מתיקיית השורש, לא מ-`GITHUB_UPLOAD`.** או למחוק את
  התיקייה כדי שלא תשמש בטעות (דורש אישור — מכילה גם `_קרא_אותי.md`).

**⚠️ טרם נפרס:** ~~v48 מוכן להעלאה~~ — **הוחלף ב-v49, ראה §5ג** (הריצות
מתוזמנות ללא משתמש; העלאה לפי §2ב דורשת אינטראקציה בדפדפן).

**ההמלצה הבאה (לפי סדר):**
1. **לפרוס את v48.** שלושה תיקוני XSS יושבים מקומית ולא מגנים על כלום עד
   שהם עולים. **מהשורש**, לא מ-`GITHUB_UPLOAD`, ועם 4 הקבצים.
2. **להדק את חוקי ה-Firestore ב-`create` של `users`** — לאמת ש-`email`
   שווה ל-`request.auth.token.email`, ולהגביל אורך `name`. זה חוסם את
   *מקור* ה-XSS ולא רק את התצוגה, ומכסה גם פידים שטרם נסרקו.
3. שלוש פונקציות מתוזמנות ב-Firebase (תפוגת מנוי, תגמולי הפניות, דוח יומי).
   מוציא את הסוכנים מהתלות בכך שהמשתמש פותח את הדף.
   עלות משוערת: 0-2 דולר לחודש. דורש מעבר ל-Blaze + Budget Alert.
4. `integrity` + `crossorigin` ל-Chart.js ול-EmailJS, אחרי אימות ה-hash
   מול cdnjs/jsdelivr.
